{
  "name": "sedona",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch app/sass/style.scss:app/css/style.css",
    "imagemin": "imagemin app/img/* --out-dir=app/img",
    "prefix": "npx postcss app/css/style.css --use=autoprefixer --output=app/css/style.css",
    "minifyCSS": "cleancss --level=1 --source-map --source-map-inline-sources -o app/css/style.min.css app/css/style.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.7.4",
    "clean-css-cli": "^4.3.0",
    "imagemin-cli": "^5.1.0",
    "node-sass": "^7.0.0",
    "postcss-cli": "^7.1.0"
  }
}
